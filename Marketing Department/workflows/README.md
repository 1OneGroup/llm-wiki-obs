# Marketing Department Workflows

Department-specific workflows that extend global harness workflows.

| Workflow | File | Purpose |
|---|---|---|
| Marketing Wiki Pipeline | `marketing-wiki-pipeline.yaml` | Ingest → generate → approve → update index |

## Global Workflows
See `harness/workflows/` for reusable global workflow definitions.
